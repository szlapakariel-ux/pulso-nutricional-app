{
  "name": "@pulso/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Tipos, contratos y utilidades compartidas entre las experiencias y la API. Placeholder en MC-1 (sin contratos de negocio todavía).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "echo \"[shared] lint placeholder — ESLint se configurará en un microciclo posterior\"",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@pulso/config": "workspace:*",
    "typescript": "^5.7.2"
  }
}
