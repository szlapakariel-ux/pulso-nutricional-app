{
  "name": "@pulso/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Componentes de interfaz compartidos entre las experiencias. Placeholder en MC-1 (sin componentes todavía).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "echo \"[ui] lint placeholder — ESLint se configurará en un microciclo posterior\"",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@pulso/config": "workspace:*",
    "typescript": "^5.7.2"
  }
}
